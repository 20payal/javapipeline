pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'javac sample_code.java'
            }
        }
        stage('Run') {
            steps {
                sh 'java sample_code'
            }
        }
    }
}